# Defaults to the production API (https://tally.useuplift.live) if unset.
# Copy to .env.local (gitignored) to point at a local dev server instead,
# e.g. when running `npx next dev` in apps/web on the same machine/simulator.
EXPO_PUBLIC_API_URL=http://localhost:3100
